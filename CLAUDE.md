@AGENTS.md

## Skills

- **`run-against-device`** — test a change against the real Miyoo Mini+; prefer `spike` + `App` unit
  tests over automating the interactive TUI
- **`add-console`** — register a new console/system (three independent tables, one easy-to-miss)
- **`install-emuhub`** — build/update the `emuhub` binary on `PATH` (use `--debug`, not release/LTO)
